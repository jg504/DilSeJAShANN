{
  "name": "dilsejashann",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "validate": "node scripts/validate-invites.mjs",
    "prebuild": "node scripts/validate-invites.mjs",
    "build": "npm run clean && astro build",
    "build:draft": "npm run clean && astro build",
    "preview": "astro preview",
    "verify": "node scripts/verify-build.mjs",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\"",
    "test": "node scripts/test-report.mjs && node scripts/test-phone.mjs",
    "check": "npm test && npm run build:draft && npm run verify"
  },
  "dependencies": {
    "astro": "^7.2.4"
  },
  "devDependencies": {
    "wrangler": "^4.125.0"
  }
}
